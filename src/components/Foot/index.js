function Foot() {
  return (
    <div className="btn-div">
      <button>按钮</button>
      {/* <button className={isPicking ? 'disabled' : ''} onClick={() => pickApple()}>{buttonText}</button> */}
    </div>
  )
}

export default Foot